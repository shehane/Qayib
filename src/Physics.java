import static java.lang.Math.PI;
import static java.lang.Math.sqrt;

public class Physics extends Subject
    {

    @Override
    public double calculateGPA()
    {
        double temp=days.size();
          return sqrt(temp*temp*PI);

    }
}
